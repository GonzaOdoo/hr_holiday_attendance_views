<odoo>
    <!-- Acción de servidor: Generar archivo IPS -->

    <record id="action_generate_ips_text" model="ir.actions.server">
        <field name="name">Exportar resumen de nomina</field>
        <field name="model_id" ref="hr_payroll.model_hr_payslip"/>
        <field name="binding_model_id" ref="hr_payroll.model_hr_payslip"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code">
            action = records.generate_payslip_pivot_excel_report()
        </field>
    </record>

</odoo>